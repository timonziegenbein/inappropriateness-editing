# Predictions Directory

This directory contains evaluation results with scorer judgments in JSONL format.

## Contents (not included in submission due to size constraints)

The full outputs include:
- Edit-level scores from local scorers (semantic similarity, fluency, human-likeness)
- Argument-level scores from global scorers (appropriateness, global human-likeness)
- Aggregated metrics and statistics
- Results from different scorer configurations (ablation studies)

## Size

Total size: ~111MB

## Generation

These files are generated by evaluating the generated edits using `models/evaluate_edits.py`:

```bash
# Evaluate with all scorers enabled
python models/evaluate_edits.py \
    --input_jsonl models/generated_edits/<model_name>.jsonl \
    --output_jsonl models/predictions/<model_name>_all.jsonl

# Evaluate with specific scorers disabled (ablation)
python models/evaluate_edits.py \
    --input_jsonl models/generated_edits/<model_name>.jsonl \
    --output_jsonl models/predictions/<model_name>_no_hl.jsonl \
    --disable_human_like
```

## File Format

Each JSONL file contains:
- `edit_scores`: Per-edit scores from local scorers
- `argument_scores`: Document-level scores from global scorers
- `perfect_edits`: Edits that passed all local scorer thresholds
- `metrics`: Aggregated statistics

## Analysis

Results can be visualized using `models/evaluation_interface.html`.

See `models/EVALUATION_WORKFLOW.md` for detailed documentation.
